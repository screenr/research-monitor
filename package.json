{
  "name": "research-monitor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "research-monitor": "./bin/research-monitor"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build": "bun build ./src/cli.ts --compile --outfile ./bin/research-monitor"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "typescript": "^5.9.0"
  }
}
